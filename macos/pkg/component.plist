<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Generated by `pkgbuild --analyze`, with BundleIsRelocatable flipped to false.

  Without this, Installer treats Elevate.app as relocatable and will "install" onto any
  Elevate.app it finds anywhere on the disk (e.g. one left in ~/Downloads) instead of putting a
  fresh copy in /Applications. The postinstall script always links /usr/local/bin/elevate to
  /Applications/Elevate.app/Contents/Helpers/elevate, so a relocated install would leave that
  link pointing at nothing.
-->
<plist version="1.0">
<array>
	<dict>
		<key>BundleHasStrictIdentifier</key>
		<true/>
		<key>BundleIsRelocatable</key>
		<false/>
		<key>BundleIsVersionChecked</key>
		<true/>
		<key>BundleOverwriteAction</key>
		<string>upgrade</string>
		<key>RootRelativeBundlePath</key>
		<string>Elevate.app</string>
	</dict>
</array>
</plist>
