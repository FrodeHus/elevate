## What this changes

<!-- A short description, and the issue it closes if there is one. -->

## Checklist

- [ ] `swift test` passes in `macos/` (ElevateCore)
- [ ] `xcodebuild ... test` passes for ElevateAppTests
- [ ] No `Elevate.xcodeproj` committed (project changes go in `macos/project.yml`)
- [ ] No real client ids, tenant ids, tokens or account names in the diff
- [ ] Documentation updated where behaviour changed (README, `macos/README.md`, `docs/`)
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` if the change is user-visible

## How it was verified

<!-- Tests added, manual steps run, screenshots. -->
