<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Elevate.App.Views.PanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="380">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header: title, offline pill, tools -->
        <Grid Grid.Row="0" Padding="14,10,8,6" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Elevate" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Border Grid.Column="1" x:Name="OfflinePill" Visibility="Collapsed" VerticalAlignment="Center"
                    Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="9" Padding="7,1">
                <TextBlock Text="offline" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Border>
            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="2">
                <Button x:Name="RefreshButton" Style="{StaticResource SubtleButtonStyle}" Width="32" Height="32" Padding="0"
                        Click="OnRefresh" ToolTipService.ToolTip="Refresh" AutomationProperties.Name="Refresh">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Body: the pivots and the list arrive in the flyout-content task; the shell shows the notice and an empty state. -->
        <StackPanel Grid.Row="1">
            <InfoBar x:Name="NoticeBar" IsOpen="False" Severity="Warning" Margin="12,0,12,8" Closed="OnNoticeClosed" />
            <TextBlock x:Name="EmptyText" Margin="14,18,14,24" FontSize="13" TextWrapping="Wrap"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Add an account to see your PIM roles." />
        </StackPanel>

        <!-- Footer -->
        <Grid Grid.Row="2" Padding="8,6" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <HyperlinkButton Grid.Column="0" Content="Add account…" Click="OnAddAccount" FontSize="13" />
            <HyperlinkButton Grid.Column="2" Content="Settings…" Click="OnSettings" FontSize="13" />
            <HyperlinkButton Grid.Column="4" Content="Quit" Click="OnQuit" FontSize="13" />
        </Grid>
    </Grid>
</UserControl>
